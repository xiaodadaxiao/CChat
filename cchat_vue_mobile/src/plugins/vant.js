import Vue from 'vue';
import { Button, Icon, Form, Field, Col, Row, Toast, CountDown, Tag } from 'vant';

Vue.use(Button)
    .use(Icon)
    .use(Form)
    .use(Field)
    .use(Col)
    .use(Row)
    .use(Toast)
    .use(CountDown)
    .use(Tag)
